apiVersion: v1
kind: ConfigMap
metadata:
  name: quiz0-config
  labels:
    app.kubernetes.io/name: quiz0
data:
  IMG_ASSET_ID: "webImageAsset"
  INSTANCE_DOMAIN: "api.topia.io"
  INSTANCE_PROTOCOL: "https"
  INTERACTIVE_KEY: "VUE7QfycF0Pob9FtYzbk"
  NODE_ENV: "production"
  PORT: "3000"
  # Legacy build-time var (Vite client calls a relative /api, so this is unused at
  # runtime); kept for parity with the ECS task def, pointed at the live dev host.
  REACT_APP_API_URL: "https://quiz0-dev-topia.topia-rtsdk.com"
